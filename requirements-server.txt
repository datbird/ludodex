# AI-forward server (HANDOFF.md §6). Install: pip install -r requirements-server.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pillow>=10.0
# AI features (phase 3) — BYOAI providers (pick per deployment; all API-key based)
anthropic>=0.114     # provider: anthropic
openai>=1.40         # provider: openai AND openrouter (OpenAI-compatible endpoint)
google-genai>=0.3    # provider: gemini
